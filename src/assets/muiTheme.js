import { createTheme } from '@mui/material';
export const theme = createTheme({
  palette: {
    general: {
      lightGreen: '#93B492',
      hoverGreen: '#6d8c6c'
    }
  }
});

export default theme;
