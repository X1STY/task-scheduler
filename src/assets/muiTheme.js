import { createTheme } from '@mui/material';
export const theme = createTheme({
  spacing: 2,
  palette: {
    general: {
      lightGreen: '#93B492',
      hoverGreen: '#758774'
    }
  }
});

export default theme;
